#!/bin/bash
# VPS 物理配置 - 本地开发环境
# 服务器: stone-Super-Server (本地)
# IP: 10.2.19.62

# ===== 基础信息 =====
NODE_NAME="local-dev"
NODE_DOMAIN="localhost"
NODE_IP="10.2.19.62"

# ===== IPv6 配置 =====
# 本地开发环境：禁用 IPv6（使用自动检测）
ENABLE_IPV6=false
IPV6_PREFIX=""
IPV6_START=1001
IPV6_COUNT=100

# ===== 端口配置 =====
RPC_PORT=9527
MONITOR_PORT=9528
WEBHOOK_PORT=9530
UTLS_PORT=8765

# ===== 性能配置 =====
CONCURRENCY=20

# ===== 日志配置 =====
LOG_LEVEL="info"

# ===== Webhook 密钥 =====
WEBHOOK_SECRET="test-secret-local-dev"

# ===== 其他节点列表（用于 webhook 转发） =====
OTHER_NODES=""

# ===== 注释 =====
# 本地开发环境配置
# 不使用 IPv6，使用自动检测
# Webhook secret: test-secret-local-dev

