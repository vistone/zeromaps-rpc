# 无 IPv6 的 VPS 配置示例
# 复制此文件为 vps-<你的IP>.conf 并修改相应参数

# 服务器基本信息
SERVER_NAME="tile-noipv6"
SERVER_DOMAIN="tile-noipv6.zeromaps.cn"
INSTALL_DIR="/opt/zeromaps-rpc"

# 服务端口
RPC_PORT=9527
MONITOR_PORT=9528

# ==========================================
# IPv6 配置（留空表示不使用 IPv6）
# ==========================================
# 如果你的 VPS 没有 IPv6 或不需要使用 IPv6，
# 请将以下变量留空或注释掉：

IPV6_PREFIX=""     # 留空 = 不使用 IPv6
INTERFACE=""       # 留空
REMOTE_IP=""       # 留空

# ==========================================
# 说明
# ==========================================
# 
# 不使用 IPv6 时的行为：
# 1. 跳过 IPv6 隧道配置
# 2. 跳过 IPv6 地址池配置
# 3. 使用 VPS 默认网络（IPv4 或系统默认 IPv6）
# 4. uTLS 代理不绑定 IPv6，使用系统路由
# 
# 优点：
# - 部署更简单
# - 适用于没有 IPv6 隧道的 VPS
# 
# 缺点：
# - 无法利用 IPv6 地址池分散请求
# - 单 IP 更容易被 Google 限制
# - 可能需要更低的并发数
# 
# 建议的环境变量：
# - UTLS_CONCURRENCY=5  # 较低的并发数

